[package]
name = "pty_t_core"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
portable-pty = "0.9"
rand = "0.9"
tokio = { version = "1", features = ["sync", "time"] }
vt100 = "0.16"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt", "time"] }
